import type { DecisionFormState } from "../types/decision";

export async function saveDecisionLocally(
  decision: DecisionFormState,
): Promise<void> {
  void decision;
  await Promise.resolve();
}
